interface:
  display_name: "CodeArts LiteLLM Setup"
  short_description: "Deploy CodeArts with LiteLLM and yolo mode."
  default_prompt: "Use $codearts-huawei-maas to install CodeArts, configure codearts-litellm through LiteLLM, and verify --yolo mode."
policy:
  allow_implicit_invocation: true
